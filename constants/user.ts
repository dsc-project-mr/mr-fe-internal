export enum Role {
  SUPER_ADMIN = 'super admin',
  DONATION_CAMPAIGN_EDITOR = 'donation campaign editor',
  ARTICLE_EDITOR = 'article editor',
  USER = 'internal user',
}

export const USER_URL = '/user/'
